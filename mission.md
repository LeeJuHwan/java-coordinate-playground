## 리팩터링 with TDD 

---

좌표 계산기 리팩터링하면서 기존 소스코드에서 어떤 부분이 변경 되었고 어떤 관점에서 작업했는지 작성하였습니다.

리팩터링 단계에서는 각 도메인의 따른 역할을 어떻게 처리할지 비즈니스 로직만 테스트하였습니다.



> ***어떻게 사용자가 입력한 좌표 값을 처리할까?***

- 기존에는 `InputHandler` 에서 반환 객체를 ₩FigureSize` 라는 `List<Coordinate>`를 관리 했었다. 이 결과 `FigureSize` 라는 객체가 담고 있는 의미가 모호해졌다.

***CoordinateStringReader*** 의 등장

좌표를 전달 받는 매개체는 다양할 것이라는 관점  -> 문자열, CSV 파일, HTTP Request 등.. 인터페이스를 구현 해서 현재 콘솔에서 받는 문자열을 처리하도록 수정하면 좋을 듯 했다.



> ***Coordinate***
> 
> 좌표 값을 포장하고 있는 VO 객체가 존재한다.

- 좌표는 '(x,y)' 형태를 준수해야한다.
- 좌표의 값은 0 이상 24 이하로 이루어져있다.
- 좌표간 연산을 수행한다.

"좌표 X 와 다른 좌표의 X 를 빼는 경우 누가 이 책임을 가져야 할까?" 를 고민하다 좌표 객체에서 연산을 하도록 책임을 변경했다.
- Point 는 좌표 값을 모르는게 정상 아닐까? -> 정상적으로 입력된 좌표 X, Y가 한 개의 점이 되는 것 뿐

기존에는 해당 좌표 객체에서 하는 행위가 없었다. 그저 일급 컬렉션을 포장하는 용도였다.

하지만, 이번에는 연산을 수행하도록 변경하니 해당 좌표에 대한 책임이 생겼다. 만약, 유지보수할 때 좌표에 대한 연산을 해야 한다면 해당 객체에게 일임하면 된다.
협력 하는 객체는 그저 메시지만 전달하도록 구성한다.

> ***Coordinates***
> 
> 좌표들을 담고 있는 일급컬렉션 포장 객체

기존 코드는 FigureSize 라는 객체가 해당 객체의 역할을 맡았다. 일급 컬렉션을 포장하면서 다른 행동이 없었는데 이번엔 좌표의 입장에서 사이즈를 검증 하고 해당 좌표를 기준으로 포인트를 생성하도록 행동을 부여했다.

결국 좌표를 읽고 처리한 CoordinatesStringReader 객체가 Coordinates 를 반환했을 때 입력한 좌표의 갯수를 기준으로 팩토리 클래스에서 도형을 생성할 수 있게된다.



> ***Point***
- 좌표의 값이 몇이든 포인트는 알 필요가 없다.
- 좌표에게 모든 연산을 하도록 메시지를 던지고 결과 값을 반환해준다.

여기서 가장 고민했던 부분은 점과 점 사이의 거리를 구하는 distanceTo 메서드이다. 처음엔 좌표간의 연산이기 때문에 Coordinate 에게 이 행동을 만들었었는데
다시 생각 해보면 점과 점 사이의 거리이기 때문에 좌표는 다른 좌표간의 거리를 구하는 개념은 몰라야 정상이지 않을까 생각이 들었다.

그래서 좌표의 연산 값을 기준으로 포인트 객체에서 거리를 구하는 방식으로 변경했는데 이렇게 구현했을 때 적절하게 책임을 분리했다고 생각한다.


> ***Points***
> - 점들을 관리할 수 있는 일급 컬렉션이 존재한다.

`getter`를 제거하고자 무수히 많은 노력을 들였지만 Rectangle 객체의 넓이를 구할 때 상, 하 대각선에 위치한 포인트 2개를 기준으로 
사각형의 넓이를 구할 수 있었다.

이 때 `getter`를 쓸 수 밖에 없었는데, 아무리 생각해봐도 안쓸 수 없지 않았을까 싶긴하다.

기존에 `Triangle`의 삼각형 넓이를 계산할 때 점과 점 사이의 거리를 구하는 메서드가 삼각형 내부에 있었지만
현재 코드에서는 점을 관리하는 배열인 `Points` 객체로 넘어왔다.


> ***Line***

- 직선인 경우 두 점 사이의 거리를 계산한다.

> ***Rectangle***

- 네 개의 점을 입력 받아 사각형의 면적을 계산한다.

> ***Triangle***

- 세 개의 점을 입력 받아 삼각형의 면적을 계산한다.
